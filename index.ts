// openclaw-plane plugin
// Plugin for Plane.so project management via the plane CLI.

export default function registerPlanePlugin(_api: any) {
  // Skills are auto-registered from openclaw.plugin.json "skills" array.
  // Future: register webhook routes, tools, or background services here.
}
